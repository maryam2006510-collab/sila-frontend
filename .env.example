# Root URL of the FastAPI backend, WITHOUT /api (API_CONTRACT.md §1: every path starts with /api)
VITE_API_URL=https://sila-backend-production-2c63.up.railway.app

# "true" serves every request from the in-browser mock, which speaks the same contract.
# Anything else talks to the real backend only; network failures surface as errors.
VITE_USE_MOCK=false
