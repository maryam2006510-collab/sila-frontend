# صِلة (Sila) · Frontend Gap Analysis & Plan

> المرجع: `Sila/Product/*` (Domain v3، Capabilities v4، System Design v1، API v2، Workflows 01 إلى 08) و `Sila/UI Kit/UI Kit/00 إلى 08`.
> تاريخ الفحص: 2026-09-24. كل ملف تحت `src/` انقرى كامل قبل كتابة هذا التقرير.

## 0. قرارات محسومة (من المستخدمة)

| الموضوع | القرار |
|---|---|
| الباكند | **جاهز لكن قيد التعديل والتجربة.** الفرونت يُبنى على `07_api_design.md` الحالي، والشغل المحلي على Mock يحاكي السيرفر (`VITE_USE_MOCK=true`) بدون أي تحويل صامت. **بعد ما يكمل الفرونت ويكمل تعديل الباكند، مريم ترسل API Design النهائي مع كامل التفاصيل والملاحظات**، وقتها نطابق `types.ts` وطبقة الـ API عليه ونحسم قسم 3. |
| الحقول الزايدة (`title_ar/en`, `seller_name`, `sparkline`, `transaction_ref`, `match_score`...) | تنضاف لعقد الـ API كإضافات موثقة (قسم 3). |
| صفحة الـ Landing | بعد ما تكمل كل الـ Flows (مرحلة 6). |
| اللغة | عربي بس حالياً، مع استخراج النصوص لملف رسائل `ar` حتى تنضاف `en` لاحقاً. زر اللغة ينخفي لين ذاك الوقت. |

---

## 1. الوضع الحالي باختصار

**الموجود والجيد (نحافظ عليه):**
- `tokens.css` مطابق للـ UI Kit تقريباً بالكامل (الألوان، الـ ramps، الـ spacing، الـ ladder، الـ radius، z-index، الحركة).
- مكوّنات `components/ui` و `components/fin` موجودة كلها بأسماء الـ Master Prompt، وأغلبها هيكلها صحيح (Button variants، StateIcon، Stepper بالـ Sila Cut، OrderSummary، SignatureCard...).
- صفحات: Role select، Signup، Login، Dashboard (مستثمر + بائع)، Market، Listing detail، Smart Match، Checkout، KYC modal، New listing، Seller listings.
- `pendingAction` store، formatters، `noOrphan`، motion tokens.
- ملفات الشعار `public/brand/*.svg` صحيحة (`fill="currentColor"` + `data-kind`).

**الخلاصة:** شغل واجهات جيد كبداية، بس **المشروع حالياً ما يشتغل أصلاً**، وكل البيانات Mock، وأغلب منطق الأعمال الحساس (المصادقة، الأخطاء، حداثة السعر) ناقص.

---

## 2. Gap Analysis

### 2.1 حرج: المشروع ما يشتغل

| # | المشكلة | الأثر |
|---|---|---|
| B1 | `index.html` يأشر على `/src/main.tsx` و **الملف مو موجود**. ماكو `App.tsx` ولا Router ولا تعريف routes. | `npm run dev` و `build` يفشلون. ولا صفحة تنعرض. |
| B2 | `fonts.ts` و `tailwind.css` ما منستوردين بأي مكان. | حتى لو اشتغل، لا خطوط ولا Tailwind. |
| B3 | ماكو `src/vite-env.d.ts`. | `tsc` يفشل: `import.meta.env` (api.ts:26). |
| B4 | كلاسات `dark:` بكل المكوّنات (Badge، OrderSummary، Topbar...) تتبع إعداد النظام (media) مو `data-theme`. | زر المظهر يغيّر التوكنات بس الحالات الملوّنة تبقى غلط. |
| B5 | مجلد `New folder (2)/AiInsight.tsx` نسخة مكررة حرفياً من `src/components/fin/AiInsight.tsx`. | ملف ميت. |

### 2.2 صفحات/Workflows ناقصة

| Route (UI Kit 08 §2.1) | Workflow | الحالة |
|---|---|---|
| `/` Landing | - | ماكو (مؤجلة لمرحلة 6) → ✅ انبنت بالمرحلة 6 |
| `/app/portfolio` | 08-أ | **ماكو** (`features/portfolio/` فارغ). `SignatureCard` و `AllocationBar` مبنيين بس ما مستخدمين |
| `/app/transactions` + receipt sheet | 08 | **ماكو** |
| `/app/premium` + Subscribe dialog | 07 | **ماكو** (`features/premium/` فارغ) |
| `/app/insights` (active + locked state) | 07 | **ماكو** (`getAiInsights()` موجود بس بدون واجهة) |
| `/app/sales` | 08-ب | **ماكو** |
| `/app/listings/:id` (بائع) | 06 | ماكو |
| `/app/settings` | - | ماكو |
| Login sheet عند انتهاء الجلسة | 08 §3.2 | ماكو |
| Suspend confirmation dialog | 06-ج / UI Kit 6.2 | ماكو (التعليق يصير بضغطة وحدة) |
| صفحة 404 + Route guards (دخول + دور) | - | ماكو |
| Mobile "المزيد" menu | UI Kit 2.2 | ماكو. Premium وسجل العمليات ما تنوصل من الموبايل |

### 2.3 عدم تطابق مع `07_api_design.md`

| # | Endpoint | الموجود بالكود | المطلوب |
|---|---|---|---|
| A1 | `POST /api/auth/signup` | ما ينادى. التسجيل `setTimeout` وهمي، و `risk_profile` ما يرسل | نداء حقيقي، `409` inline على الإيميل |
| A2 | `POST /api/auth/login` | ما ينادى. يحط توكن ثابت `mock_access_token_jwt_991823` | نداء حقيقي + تخزين access/refresh |
| A3 | `POST /api/auth/refresh` | ماكو | تجديد صامت عند `401` ثم إعادة الطلب، وإذا فشل: login sheet مع حفظ الـ intent |
| A4 | `GET /api/listings` | ما يرسل أي فلتر. الفلترة والترتيب بالفرونت، وفيه بحث نصي مو موجود بالـ API | إرسال `karat`, `min_price`, `max_price`, `sort=promoted_first` ومزامنتها مع الـ URL |
| A5 | `GET /api/listings?seller_id=me` | صفحة "عروضي" تعرض **كل عروض السوق** مو عروض البائع | فلترة سيرفرية بحساب البائع |
| A6 | `POST /api/ai/risk-analysis` | ما ينادى أبداً. الكود يعتمد على `ai_risk_insight` داخل الـ preview | نداء بالتوازي مع `preview` (Workflow 05 خطوة 2) |
| A7 | `GET /api/subscription/status` | ما ينادى | Portfolio + Premium |
| A8 | `GET /api/transactions/{id}` | ما ينادى | receipt sheet |
| A9 | `/api/transactions/confirm` | بدون idempotency key | header لكل محاولة تأكيد (UI Kit 3.4) |
| A10 | كل الطلبات | أي خطأ شبكة يتحول **بصمت** للـ Mock (api.ts:88-94) | خطأ حقيقي يظهر للمستخدم. تأكيد شراء فشل بالشبكة لازم يعرض "راجِع سجل العمليات" مو نجاح وهمي |
| A11 | `setInterval` للأسعار الوهمية | يشتغل عند تحميل الموديول حتى مع باكند حقيقي | ينحصر داخل طبقة الـ Mock |

### 2.4 منطق أعمال غلط أو ناقص

| # | المشكلة | المكان | الصح حسب المستندات |
|---|---|---|---|
| L1 | حد العمولة: `>= 200` يعطي 0.5% | `api.ts:161` | 50 إلى 200 غ (شامل 200) = 1.0%، **أكثر من** 200 = 0.5% |
| L2 | Mock confirm يسجل عمولة 1.5% ثابتة لكل الصفقات | `api.ts:204-213` | حسب الشريحة |
| L3 | التقدير بصفحة التفاصيل والـ Checkout يستخدم `base_price_per_gram` (سعر وقت إنشاء العرض) | `ListingDetailPage:78`, `CheckoutPage:218` | سعر حي: `price_24k × karat/24`، و `base_price` مرجع بس |
| L4 | قيمة المحفظة = `إجمالي الغرامات × سعر عيار 24` | `DashboardHome:61` | تضخيم للقيمة إذا المحفظة فيها 21/18. لازم حسب العيار من سجل الصفقات |
| L5 | شاشة النجاح تعرض رصيد **ثابت بالكود** "37.750 غرام" | `CheckoutPage:334` | الرصيد الجديد من السيرفر |
| L6 | حداثة المعاينة: `isStale` موجود بس ما ينحسب أبداً | `OrderSummary`, `CheckoutPage` | بعد 60 ثانية أو تغيّر السعر: زر التأكيد يتبدل بـ "حدّث المعاينة" |
| L7 | `INSUFFICIENT_AVAILABLE_WEIGHT` → toast ويرجع لخطوة الكمية | `CheckoutPage:106` | رسالة inline بالملخص بالكمية الجديدة + "عدّل الكمية" |
| L8 | `LISTING_NOT_ACTIVE` وخطأ الشبكة → toast عام | `CheckoutPage:110` | شاشة كاملة "العرض لم يعد متاحاً" / "راجِع سجل العمليات" |
| L9 | KYC modal: أي خطأ بـ `/kyc/*` يعلّق الشاشة على spinner للأبد (ماكو try/catch)، والتأخير مصطنع (600+800+600ms) | `KycModal:30-47` | تقدم حقيقي، حد أدنى 890ms، وحالة فشل |
| L10 | Smart Match: الملف الاستثماري مكتوب "متوسط" ثابت، وفشل الـ AI يعرض "ميزانيتك قليلة"، وأقل سعر "84,000" مكتوب بالكود | `SmartMatchPage:73,40,227` | من حساب المستخدم، حالة "غير متاحة مؤقتاً" منفصلة، والرقم من السيرفر |
| L11 | Smart Match يوديك للـ Checkout بغرامات ممكن أكبر من المتاح، والـ Checkout يقصّها بصمت (`Math.min`) | `SmartMatchPage:201`, `CheckoutPage:168` | Validation ظاهر: "الحد الأقصى المتاح 84.250 غ." |
| L12 | صفحة تفاصيل العرض تعرض نص AI **مكتوب بالفرونت** كأنه تحليل | `ListingDetailPage:155` | إزالته. الـ AI يظهر بس من استجابة السيرفر |
| L13 | KPIs وهمية: `+0.84%` و `+4.8%` بالمحفظة، مبيعات البائع `50 غ` و `5,625,700` ثابتة | `DashboardHome` | من البيانات أو `-` |
| L14 | Premium يُفحص بـ `subscription_tier === 'premium'` | `Sidebar:45` | القاعدة: `subscription_expiry_date > now()` |
| L15 | رسم الترويج (25,000) ومدته (7 أيام) مكتوبة بالفرونت | `SellerListingsPage` | Config سيرفري (قسم 3) |
| L16 | المستخدم الافتراضي بالـ Layout هو `DEMO_INVESTOR` الموثّق والـ Premium | `AppLayout:17` | من `/api/users/me`، وإلا ما يوصل مسار الـ KYC أبداً |
| L17 | الرسوم البيانية كلها مولّدة بـ `Math.random` | `LivePricePanel`, `ListingDetailPage` | endpoint تاريخ أسعار (قسم 3) |
| L18 | Polling الأسعار كل 5 ثواني يدوياً | `AppLayout:38` | كل 30 ثانية + عند رجوع التركيز، وتحذير "آخر سعر متاح" بعد 90 ثانية |

**تعارض بالمستندات نفسها (يحتاج قرار، انظر قسم 5):**
Workflow 02 (ب، خطوة 5) يگول البائع **يعيد الضغط** على نشر بعد التوثيق، بينما Workflow 06 (أ، خطوة 3) و UI Kit 08 (§5 Sheet 2.3) يگولون **ينشر تلقائياً**. الكود الحالي يطلب إعادة الضغط.

### 2.5 تناقض مع الـ Domain Model

- **AssetListing:** الكود يضيف `title_ar`, `title_en`, `seller_name`, `seller_verified`, `sparkline`. ماكو عنوان بالـ Domain أصلاً.
- **Transaction:** يضيف `karat`, `seller_name`, `buyer_name`, `transaction_ref`. عرض اسم المشتري للبائع لازم يكون مجهّل (UI Kit 11 §8.2).
- **FractionalOwnershipRecord:** يضيف `verified`. وماكو معالجة لـ `INTEGRITY_CHECK_FAILED` (لازم ما ينعرض أي رقم).
- **MarketPrices:** `change_24h_pct` مو موجود بالـ API doc.
- **User:** الحقول مطابقة (`subscription_tier`, `subscription_expiry_date` موجودين) بس الاستخدام غلط (L14).
- **حقول الكوميشن بالـ Transaction:** موجودة ومطابقة (`principal_amount`, `commission_rate`, `commission_amount`, `total_paid_by_investor`).

### 2.6 مخالفات الـ UI Kit

| # | المخالفة | أمثلة |
|---|---|---|
| K1 | Emoji ممنوع | `💡` بـ `CheckoutPage:175` |
| K2 | Hex خارج `tokens.css` | `PriceChart.tsx:37-67` |
| K3 | أكثر من زر ذهبي بالشاشة | كل `ListingCard` بيه "اشترِ الآن" ذهبي (شبكة كاملة ذهبية)، الداشبورد، KYC، شاشة النجاح. والذهب مستخدم لعناوين عند hover، نسب العمولة، نصوص عادية |
| K4 | خطوط أصغر من الحد الأدنى (14px عربي) وقيم عشوائية | `text-[11px]`, `text-[12px]`, `text-[13px]` عربي بعشرات الأماكن، وأحجام خارج السلم (15، 18، 22، 30، 34) |
| K5 | Arbitrary values بكل الكلاسات بدل tokens | `h-[48px]`, `text-[16px]`, `rounded-[var(--radius-sm)]`... الـ Tailwind `@theme` ما يعرّف السلم |
| K6 | أوزان وأحجام أيقونات ممنوعة | `weight="bold"` (LockSimple، Check، Plus)، أحجام 14/15/18/22/26/28/36/40/44 بدل 16/20/24/32/52، `Hourglass animate-spin`، `Sparkle` كزينة بالأزرار (مسموح بس بسطر الـ AI) |
| K7 | RTL mirroring غلط | `ArrowRight className="rotate-180"`، أسهم الرجوع ما تنعكس حسب الاتجاه |
| K8 | دوائر بدل مربعات | نقاط `rounded-full` (TierTable، RoleSelect، إشعار الجرس)، دائرة زر الإضافة بالموبايل، دوائر أيقونات النجاح/الـ KYC |
| K9 | الشعار | `<img>` + CSS filter hacks بدل SVG inline بـ `currentColor`. وبصفحات Login/Signup شعار كحلي على خلفية كحلية بالوضع الليلي (**يختفي**) |
| K10 | Login و Signup مو T4 (golden split + brand panel) | صفحات وسطية |
| K11 | Glass/blur بالتطبيق | Topbar `backdrop-blur-md`، Dialog `backdrop-blur-xs` (مسموح بس بـ nav الـ Landing) |
| K12 | أرقام عربية شرقية | `Intl.NumberFormat('ar-IQ')` بدون `numberingSystem: 'latn'` بـ `OrderSummary:121` و `SmartMatchPage:125` |
| K13 | `RollingNumber` ما يدوّر فعلياً | الـ key `${index}-${char}` يعيد إنشاء الرقم بدل تحريكه، و `aria-live` على كل نسخة |
| K14 | نصوص ممنوعة/مضللة | "مرخصة ومطابقة للمعايير المالية" (`RoleSelectPage:61`) ادعاء غير صحيح للـ MVP، "الأكثر شفافية"، "بدقة متناهية" |
| K15 | KYC sheet | بدون شعار صِلة بالـ header، والزر الرئيسي ذهبي بدل Primary، ونص "محاكاة" مو بالصيغة المطلوبة |
| K16 | Sparkline بالوضع النهاري | لون `accent-400` على أبيض (يفشل التباين). المطلوب `accent-700` |
| K17 | Tooltip | `start-1/2 -translate-x-1/2` يخرب بالـ RTL، وكلاس `animate-in` مو موجود |

### 2.7 مشاكل تقنية عامة

- **TanStack Query مثبت وما مستخدم.** كل الجلب `useEffect` يدوي بدون معالجة أخطاء (`Promise.all` بدون `catch` → Checkout يعلق على "جارٍ تحميل" للأبد إذا فشل الطلب).
- **مصادر متعددة لبيانات المستخدم:** AppLayout، CheckoutPage، NewListingPage (الأخيرة تبدأ بـ `DEMO_SELLER`).
- **3 نسخ من `KycModal`** (Layout، Checkout، NewListing).
- **React Hook Form + Zod مثبتين وما مستخدمين.**
- `Input` يولّد `id` بـ `Math.random()` بكل render (يكسر ربط الـ label). الحل `useId`.
- التوكن بـ `localStorage` (System Design §5 يطلب تخزين آمن).
- `animejs`, `gsap`, `@gsap/react` غير مستخدمة لحد الآن (نخليهن: Landing و الحركة).
- ماكو ESLint / Prettier / Vitest، ولا `.env.example`.
- المشروع **مو git repo**، فما نكدر نسوي commits لحد ما ننشئه.

---

## 3. إضافات مقترحة لعقد الـ API (للفريق الباكند)

> حسب قرار "نضيفها لعقد الـ API". قبل ما نثبتها، نسحب `GET /openapi.json` من الباكند الجاهز ونطابق: إذا الحقل موجود باسم مختلف نستخدم اسمه، وإذا مو موجود ينطلب.

| Endpoint | إضافة |
|---|---|
| `GET /api/listings`, `/listings/{id}` | `title_ar`, `title_en`, `seller_name` (أو معرّف مجهّل)، `seller_verified`, `sparkline_24h: number[]` |
| `GET /api/market/prices` | `change_24h_pct`, `last_updated` |
| **جديد** `GET /api/market/prices/history?karat=&range=1D\|1W\|1M\|3M\|1Y` | بيانات الرسوم البيانية (حالياً عشوائية) |
| `POST /api/transactions/confirm` | يرجع `transaction_ref` + `total_accumulated_grams` الجديد (شاشة النجاح) |
| `GET /api/transactions` | `karat`, `transaction_ref`, `seller_name` (للمستثمر)، `buyer_display` مجهّل (للبائع) |
| `GET /api/ownership/me` | `digital_signature_token` (أو نسخة مختصرة)، `updated_at` |
| `POST /api/ai/match` | `ai_reason`, `match_score` لكل نتيجة، `summary`, `min_available_price_per_gram` (حالة الميزانية القليلة) |
| **Config** (endpoint جديد أو ضمن الموجود) | رسم الترويج ومدته، سعر Premium الشهري (UI Kit يطلب عرضها قبل الدفع، والـ API يمنع تعديلها من الـ client بس ما يوفر قراءتها) |

الناتج: ملف `API_ADDITIONS.md` يتسلم للباكند.

---

## 4. خطة التنفيذ (مرحلة وحدة كل مرة)

الأولوية: 🔴 حرج (يكسر وظيفة أساسية) · 🟠 مهم (منطق أعمال / مطابقة) · 🟡 تحسين.
بعد كل مرحلة: `npm run build` + تشغيل محلي + commit واضح، وأوقف أنتظر تأكيدك قبل المرحلة الجاية.

### المرحلة 0 · خلّي المشروع يشتغل 🔴
| البند | الحل | الملفات |
|---|---|---|
| B1, B2 | إنشاء `main.tsx` (يستورد fonts + tailwind.css + Providers) و `app/router.tsx` بكل routes الـ UI Kit 2.1، الصفحات الناقصة placeholder مؤقت | `src/main.tsx`, `src/app/router.tsx`, `src/app/App.tsx` |
| B3 | `vite-env.d.ts` مع تعريف `VITE_API_URL`, `VITE_USE_MOCK` | `src/vite-env.d.ts`, `.env.example` |
| B4 | `@custom-variant dark (&:where([data-theme=dark], [data-theme=dark] *));` | `src/styles/tailwind.css` |
| B5 | حذف `New folder (2)/` (بعد موافقتك) | - |
| - | `git init` + `.gitignore` + أول commit للحالة الحالية كـ baseline (بعد موافقتك) | - |

**معيار الإنجاز:** `tsc` نظيف، `npm run build` ينجح، كل الصفحات الموجودة تنفتح.

**الحالة: ✅ مكتملة (2026-09-24).** ملاحظات:
- `git` مو منصّب على الجهاز، فالـ `git init` والـ commits معلّقة لين ينصّب.
- انصلح خلل إضافي بالـ Mock: `GET /listings/{id}` كان يرجّع مصفوفة كاملة، فتنكسر صفحة التفاصيل والـ Checkout.
- مؤجل للمرحلة 2: حجم الـ bundle (958kB، يحتاج code splitting)، الخطوط تحمّل subsets زايدة (cyrillic, vietnamese)، وشعار TradingView ينحقن بالرسم البياني (`attributionLogo`).

### المرحلة 1 · طبقة الـ API والجلسة 🔴
| البند | الحل | الملفات |
|---|---|---|
| A10, A11 | فصل `lib/api/http.ts` (fetch + توحيد `ApiError`) عن `lib/api/mock/` (handlers فقط لو `VITE_USE_MOCK=true`). حذف التحويل الصامت | `src/lib/api.ts` → `src/lib/api/*` |
| A1-A3 | auth endpoints + refresh تلقائي عند `401` + login sheet مع `pendingAction` | `lib/api/auth.ts`, `lib/session.ts`, `features/auth/*` |
| - | مطابقة `types.ts` مع `openapi.json` الحقيقي + كتابة `API_ADDITIONS.md` | `src/lib/types.ts`, `API_ADDITIONS.md` |
| - | TanStack Query hooks: `useMe`, `useMarketPrices` (30s + focus)، `useListings(filters)`، `useOwnership`، `useTransactions`، `useSubscriptionStatus` | `src/lib/queries/*` |
| L16 | Route guards (مسجل دخول + حسب الدور) بدل `DEMO_INVESTOR` | `app/router.tsx`, `features/shell/AppLayout.tsx` |
| - | `KycModal` واحد عام يقرأ `pendingAction` | `features/kyc/*` |
| A9 | Idempotency key على confirm | `lib/api/transactions.ts` |
| - | أزرار "دخول تجريبي" و "تبديل الدور" تنعرض بس بوضع الـ Mock | `LoginPage`, `Sidebar` |

**الحالة: ✅ مكتملة (2026-09-24).**
- طبقة الـ API صارت `src/lib/api/` (`http.ts` مسار واحد: transport ← 401 ← refresh مرة وحدة ← إعادة الطلب، `errors.ts`، `tokens.ts`، `client.ts`). حُذف `src/lib/api.ts`.
- الـ Mock صار `src/lib/mock/server.ts`: يحاكي السيرفر بالكامل (توكنات، RBAC، فحص KYC، الكميات، شرائح العمولة الصحيحة، انتهاء الاشتراك، idempotency) ويتحمّل كـ chunk منفصل بس لما `VITE_USE_MOCK=true`. حساب تجريبي جديد: `زينب الموسوي` (مستثمرة غير موثقة، Free).
- TanStack Query (`src/lib/queries.ts`)، route guards (`RequireAuth`, `RoleRoute`)، نافذة "انتهت الجلسة"، KYC modal واحد عام (`openKyc()`).
- انحلّت ضمنياً: A1, A2, A3, A5, A9, A10, A11، L1, L2 (بالـ Mock)، L5، L9 (المنطق، التصميم بالمرحلة 3)، L14، L16، L18 (الـ polling)، وقرار §5-1 (نشر تلقائي بعد توثيق البائع).
- اختبار آلي بمتصفح headless: 14 سيناريو ناجح بدون أخطاء runtime (guards، دخول/خروج، refresh بعد reload، شراء مع KYC بدون تنفيذ تلقائي، 409 بالتسجيل، نشر البائع مع KYC، RBAC)، وتأكيد إن الوضع الحقيقي بدون باكند يعرض خطأ اتصال بدل بيانات وهمية.
- **ما انختبر بعد:** نافذة انتهاء الجلسة (تحتاج توكن refresh مرفوض أثناء الاستخدام).

**نقاط لازم تتأكد وقت وصول الـ API Design النهائي:**
1. شكل `POST /auth/refresh`: الكود يرسل `{ refresh_token }` بالـ body. إذا الباكند يتوقعه بالـ header أو httpOnly cookie نعدّل `http.ts` و `tokens.ts`.
2. تخزين التوكن: حالياً access بالذاكرة و refresh بـ `localStorage`. إذا الباكند يدعم httpOnly cookie نتحول له (System Design §5).
3. رد `POST /auth/signup`: الكود يفترض يرجّع `User` بدون توكنات، فيسوي login بعده.
4. أكواد HTTP لأخطاء الأعمال: الكود يعتمد على `error_code` مو الـ status، بس الـ Mock يستخدم `409` لـ `INSUFFICIENT_AVAILABLE_WEIGHT` و `LISTING_NOT_ACTIVE`.
5. دعم header `Idempotency-Key` على `/transactions/confirm`.
6. `GET /listings?seller_id=me` لعروض البائع.
7. هل `preview` يرجّع `ai_risk_insight` لو الـ AI منفصل بـ `/ai/risk-analysis` فقط؟
8. الحقول الإضافية بقسم 3.

### المرحلة 2 · أساس التصميم (قبل بناء أي شاشة جديدة) 🟠
| البند | الحل | الملفات |
|---|---|---|
| K5, K4 | تعريف سلم الخطوط، الـ spacing، الـ radius، الارتفاعات، الـ ladder بـ `@theme` حتى نكتب `text-h3 h-lg rounded-sm` بدل القيم العشوائية. تثبيت حد 14px عربي | `tailwind.css` + كل `components/ui` |
| K9 | `Logo` يعرض SVG inline بـ `currentColor` | `components/ui/Logo.tsx` |
| K12, K13 | كل الأرقام تمر من `formatters.ts` (أرقام غربية)، إصلاح `RollingNumber` (key ثابت حسب الموقع، `aria-live` اختياري ومقيّد) | `lib/formatters.ts`, `components/ui/RollingNumber.tsx`, `Num.tsx` |
| K2, K16 | `PriceChart`/`Sparkline` تقرأ الألوان من CSS variables | `components/fin/PriceChart.tsx`, `Sparkline.tsx` |
| K6, K7, K8 | الأيقونات: regular/fill بس، أحجام الـ tokens، `mirrored` حسب الاتجاه، مربعات بدل الدوائر | عدة ملفات |
| K11, K17 | إزالة الـ blur من التطبيق، إصلاح Tooltip للـ RTL، `Input` بـ `useId` | `Topbar`, `Dialog`, `Tooltip`, `Input` |
| - | استخراج النصوص لـ `src/i18n/ar.ts` + إخفاء زر اللغة | `src/i18n/*`, `Topbar` |
| - | أداة `lib/pricing.ts` مشتركة: `karatPrice()`, `commissionRate()` بالحدود الصحيحة، `estimateOrder()` + unit tests | `src/lib/pricing.ts` |

**الحالة: ✅ مكتملة (2026-09-24).**
- `tailwind.css`: كل توكنز الكيت صارت utilities (`bg-surface-1`, `text-h3`, `h-control-lg`, `h-g4`, `rounded-md`, `z-modal`...) مع تصفير الـ scales الافتراضية لـ Tailwind. توكنز جديدة بـ `tokens.css`: الحالات (success/danger/warning/info)، الأزرار، الرسوم، ألوان العيارات، السايدبار، لون الشعار.
- كل `components/ui` و `components/fin` و `features/shell` انعادت كتابتها على التوكنز: **صفر** قيم عشوائية، صفر hex، صفر `dark:` مع rgba، أيقونات regular/fill بس وبأحجام التوكنز، مربعات بدل الدوائر، بدون blur.
- الشعار SVG inline بـ `currentColor` (نسخ بدون metadata بـ `src/assets/brand`)، مع `tone="auto"`.
- `formatters.ts`: أرقام غربية دائماً، علامة ناقص حقيقية (−)، `fmtRate`، تواريخ بأسماء الأشهر العراقية.
- `RollingNumber` صار يدوّر فعلاً (key ثابت من اليمين)، والإعلان لقارئ الشاشة مقيّد كل 10 ثواني.
- i18n: `src/i18n/ar.ts` + `useT()`؛ اللغة مقفولة على العربي وزر اللغة انشال.
- الأيقونات كلها صارت `*Icon` (الأسماء القديمة deprecated).
- تقسيم الـ bundle: الصفحات lazy routes، الرسوم البيانية (173KB) ما تنحمّل إلا بصفحاتها.
- **أخطاء حقيقية انكشفت وانصلحت:**
  - الخط `Montserrat` ما كان يشتغل أبداً (اسمه المسجّل `Montserrat Variable`)، فكل الأرقام كانت بخط بديل وبدون `tnum`.
  - `base.css` كان خارج طبقات Tailwind فيغلب الـ utilities (مثلاً `truncate` على العناوين).
  - ماكو تسجيل خروج بالموبايل؛ انضاف بقائمة "المزيد".
  - "%" تنقلب داخل النص العربي؛ انحلّت بـ `<bdi>` و `isolateFigures()`.
- `DECISIONS.md` انضاف (9 قرارات).
- الاختبار: 14/14 سيناريو، صفر أخطاء runtime، ولقطات شاشة (desktop/mobile، dark/light) انراجعت.
- **مؤجل:** تصغير الـ bundle الرئيسي (210KB gzip) بـ `LazyMotion` (مرحلة 5)؛ حركة رسم الـ sparkline بـ Anime.js (مرحلة 7)؛ unit tests لـ `pricing.ts` (مرحلة 5، تحتاج Vitest).

### المرحلة 3 · تصحيح الـ Flows الموجودة 🟠
| Flow | البنود | الملفات |
|---|---|---|
| 01 Onboarding | Signup/Login بالـ API الحقيقي، T4 layout، RHF + Zod، `409` inline، rate-limit banner، شطب K14 | `features/auth/*` |
| 03 Market | فلاتر سيرفرية بالـ URL (A4)، زر "اشترِ" مو ذهبي بالبطاقات (K3)، تقدير بالسعر الحي (L3)، إزالة الـ AI الوهمي (L12)، حالة تحذير السعر القديم (L18) | `features/market/*`, `ListingCard`, `LivePricePanel` |
| 04 Smart Match | الملف الاستثماري من المستخدم، حالة "AI غير متاح"، أقل سعر من السيرفر، إزالة Sparkle من الزر (L10, K6) | `features/match/*` |
| 05 Checkout | `risk-analysis` بالتوازي (A6)، validation ظاهر (L11)، حداثة 60 ثانية + تغيّر السعر (L6)، كل حالات الأخطاء (L7, L8)، رصيد النجاح من السيرفر (L5)، شطب الـ emoji (K1) | `features/checkout/*`, `OrderSummary` |
| 02 KYC | try/catch + حالة فشل، حد أدنى 890ms بدون تأخير مصطنع، header بشعار صِلة، الزر Primary، الرجوع لنفس الخطوة (L9, K15) | `features/kyc/*` |
| 06 Seller | "عروضي" بفلترة البائع (A5)، Suspend dialog، رسم الترويج من Config (L15)، النشر بعد التوثيق حسب قرارك بقسم 5 | `features/seller/*` |
| Dashboard | إزالة KPIs الوهمية (L13)، قيمة المحفظة حسب العيار (L4) | `DashboardHome` |

**الحالة: ✅ مكتملة (2026-09-24).**
- **01 Onboarding:** اختيار الدور والتسجيل والدخول صاروا T4 (golden split + brand panel، و header band بالموبايل). React Hook Form + Zod بنفس قواعد السيرفر، وأول حقل غلط ياخذ الـ focus. `409` تحت الإيميل، و rate limit بعدّاد تنازلي. انشالت "مرخصة ومطابقة للمعايير المالية" (K14). الملف الاستثماري صار اختيار إجباري.
- **Dashboard:** قيمة المحفظة حسب العيار (L4)، KPIs حقيقية بدل الوهمية (L13)، بطاقة "ابدأ من هنا" تتأشر تلقائياً، و empty state للبائع بسعر عيار 21 المرجعي.
- **03 Market:** فلتر العيار والسعر (من/إلى) سيرفري ومتزامن مع الـ URL (A4)، والبحث النصي انشال لأن مو بالـ API. empty state و error state بنفس حجم البطاقة.
- **تفاصيل العرض:** T3، السعر الحي للعيار بدل المرجعي (L3)، تقدير شفاف بالعمولة، رسالة واضحة لو العرض غير نشط، والبائع ما يشوف زر الشراء.
- **04 Smart Match:** الملف الاستثماري من الحساب، حالة "AI غير متاح" منفصلة، أقل سعر من السيرفر مع زر "اجعل الميزانية بالحد الأدنى"، والغرامات ما تتجاوز المتاح (L10, L11).
- **05 Checkout:** مدة صلاحية المعاينة 60 ثانية + كشف تغيّر السعر (L6)، `/ai/risk-analysis` بالتوازي (A6)، كل حالات الأخطاء (L7, L8, الشبكة بدون إعادة تلقائية)، منع مغادرة الصفحة أثناء التنفيذ، استرجاع الكمية بعد reload من `pendingAction`، إيصال كامل برقم قابل للنسخ، وشريط سفلي ثابت بالموبايل.
- **02 KYC:** header بشعار صِلة، زر Primary، الختم regular → fill، وحالة فشل.
- **06 Seller:** نموذج بـ validation ومعاينة بطاقة حقيقية، نافذة تأكيد التعليق، نافذة ترويج تعرض الرسم والمدة قبل الدفع (`serverConfig.ts` لين يوصل Config endpoint)، ما في حذف (مع شرح)، وبطاقات بالموبايل.
- الاختبار: **17/17** سيناريو (ضفنا فلتر السوق، نتائج المطابقة، الميزانية الأقل من الحد)، صفر أخطاء runtime، ولقطات شاشة انراجعت. `DECISIONS.md` صار بيه 12 قرار.
- **ما انختبر آلياً:** انتهاء صلاحية المعاينة بعد 60 ثانية، و `INSUFFICIENT_AVAILABLE_WEIGHT` (يحتاج مشتري ثاني بنفس اللحظة).

### المرحلة 4 · الشاشات الناقصة 🟠
| الشاشة | Workflow | ملاحظات |
|---|---|---|
| Portfolio `/app/portfolio` | 08-أ | T5، رصيد موثّق + SignatureCard، حالة `INTEGRITY_CHECK_FAILED` بدون أي رقم، توزيع حسب العيار، ملخص الاشتراك، حالة "ابدأ أول استثمار" |
| Transactions `/app/transactions` | 08 | جدول + receipt sheet (`GET /transactions/{id}`)، "سجل العمليات دائم ولا يُحذف" |
| Premium `/app/premium` + Subscribe dialog | 07 | حساب تاريخ البداية/النهاية نفس السيرفر (تمديد من الانتهاء الحالي)، شارة "دفع تجريبي" |
| Insights `/app/insights` | 07 | نشط، أو locked state بالهيكل فقط عند `SUBSCRIPTION_REQUIRED` |
| Sales `/app/sales` | 08-ب | KPIs حقيقية، المبلغ المستلم "بدون خصم عمولة"، مشتري مجهّل |
| Seller listing detail `/app/listings/:id` | 06 | |
| Settings، 404، Mobile "المزيد" | - | Premium وسجل العمليات تنوصل من الموبايل |

**الحالة: ✅ مكتملة (2026-09-24).**
- **محفظتي (T5):** رصيد موثّق كنقطة تركيز + القيمة حسب العيار + تغيّر اليوم، بطاقة التوقيع، التوزيع حسب العيار، رصيدك عبر الزمن (من سجل الشراء الحقيقي، مو بيانات مولّدة)، ملخص الاشتراك مع تنبيه قبل الانتهاء بـ 3 أيام، وسجل العمليات. عند `INTEGRITY_CHECK_FAILED` تنخفي كل أرقام الرصيد (يمكن محاكاتها بالـ Mock: `localStorage.setItem('sila-mock-integrity-fail','1')`).
- **سجل العمليات** (للدورين): جدول بالديسكتوب وصفوف مختصرة بالموبايل، والنقر يفتح إيصال من `GET /transactions/{id}`. المستثمر يشوف المدفوع، والبائع يشوف المستلم والمشتري مجهّل.
- **Premium:** عرض بالسعر والمزايا، ونافذة اشتراك تحسب التواريخ مثل السيرفر (التجديد المبكر يمدد من تاريخ الانتهاء). **Insights:** نشطة، أو حالة مقفولة تعرض الهيكل بس (بدون بيانات مموّهة) عند `SUBSCRIPTION_REQUIRED`.
- **سجل المبيعات، وتفاصيل عرض البائع** (مع مبيعاته)، و **الإعدادات** (الحساب، التوثيق، المظهر، الاشتراك، ملاحظة إن التوقيع الرقمي إثبات داخلي مو بديل عن التسجيل الحكومي حسب System Design §6، وإسناد TradingView).
- مكوّن `ReceiptDetails` مشترك بين إيصال الشراء والسجل. انحذف `PlaceholderPage`.
- العربي: صيغة المعدود صحيحة للأيام (يوم واحد، يومان، 3 إلى 10 أيام، 11+ يوماً).
- الاختبار: **22/22** سيناريو، صفر أخطاء runtime.

### المرحلة 5 · الجودة 🟡
- ESLint + Prettier + Vitest (tests لـ `pricing.ts`, formatters, `pendingAction`).
- بوابات الـ QA من الـ Master Prompt §7 (grep للـ hex، gradient، الشرطات، `left/right`).
- مراجعة الـ checklists بملفات الـ UI Kit 01 إلى 08 لكل شاشة.

**الحالة: ✅ مكتملة (2026-09-24).**
- **ESLint** (typescript-eslint + react-hooks v7 بقواعد React Compiler + react-refresh): صفر ملاحظات. انصلح 25 شي حقيقي:
  - قراءة الوقت أثناء الـ render (`purity`) صارت عبر `useNow()` و `lib/status.ts`، فالصفحة تتحدث لو انتهى اشتراك أو ترويج وهي مفتوحة.
  - نسخ الـ state داخل effects (`set-state-in-effect`) صار بنمط "adjust during render" أو `key`.
  - الـ helpers انفصلت عن ملفات المكوّنات (Fast Refresh).
- **Prettier:** كل الكود بنمط واحد.
- **Vitest (54 اختبار، ~7 ثواني):** التسعير وحدود شرائح العمولة، المنسّقات والأرقام الغربية، عزل الأرقام بالنص العربي، مسار الطلبات (refresh مشترك، انتهاء الجلسة، خطأ الشبكة)، قواعد السيرفر عبر الـ Mock (KYC، RBAC، idempotency، نفاد العرض، تمديد الاشتراك)، ومكوّنات (ملخص الطلب، MoneyInput).
- **Playwright (23 اختبار):** 22 رحلة ديسكتوب من 01 لين 08 + اختبار موبايل (الشريط السفلي، "المزيد"، ماكو scroll أفقي)، مع التأكد من صفر أخطاء runtime.
- **بوابات الـ UI Kit** (`npm run qa`): 11 فحص آلي، كلها ناجحة.
- **الأداء:** `LazyMotion` + صفحة الدخول lazy: الحمولة الأولى من 245.8 إلى 185.4 KB gzip.
- `npm run check` يجمع كل شي، و `README.md` بيه طريقة التشغيل والحسابات التجريبية.
- **ما انعمل:** Lighthouse (يحتاج بيئة Chrome/CI)، واختبارات RTL/LTR (الإنكليزية مؤجلة).

### المرحلة 6 · Landing Page
حسب UI Kit 08 §12 و 07 §4 (GSAP بـ chunk منفصل، walkthrough مثبت، قسم أفقي واحد، الوضع الليلي فقط). تنبني بمرحلة مستقلة بعد اكتمال الـ Flows.

**الحالة: ✅ مكتملة (2026-09-24).**
- `src/features/landing/`: Nav (زجاجي بعد النزول، بدون scroll listeners)، Hero (بطاقة السعر الحقيقية + دخول SplitText بالكلمات)، شريط أسعار (الـ marquee الوحيد، يوقف لما يطلع من الشاشة)، "كيف تعمل" (pin بالديسكتوب بخمس شاشات تجريبية، وقائمة عادية بالموبايل وتقليل الحركة)، حاسبة أسعار بنفس معادلة السيرفر وجدول الشرائح وسبائك تتكدس (Anime.js)، طبقات الأمان (القسم الأفقي الوحيد، و carousel بالموبايل)، تركيب الشعار، للبائعين، Premium bento، أسئلة شائعة، CTA أخير و footer.
- GSAP بس بـ chunk الـ Landing (75 KB gzip)؛ الـ bundle الرئيسي ما تغيّر (187 KB gzip).
- `prefers-reduced-motion`: ماكو pin ولا scrub، وكل المحتوى يبين مباشرة.
- قرارات جديدة: D15 (حجم عنوان الـ Hero)، D16 (الشعار ما يتكرر بنفس الشاشة).
- اختبارات: `e2e/landing.spec.ts` (العنوان، marquee واحد، صفر overflow أفقي، صفر أخطاء، الـ CTA يودّي لـ `/signup`، وتقليل الحركة). المجموع 26 E2E + 54 unit + 11 QA gate، كلها ناجحة.
- معروف: بالموبايل (390px) عنوان الـ Hero يطلع 3 أسطر متوازنة؛ قاعدة "سطرين" مطبّقة من التابلت وفوق.

### المرحلة 7 · Polish
جولات الـ UI Polish ضمن توكنات الـ Kit فقط.

**الحالة: ✅ مكتملة (2026-09-24).** سبع جولات. الجولة الأخيرة الشاملة (كل الشاشات، الديسكتوب نهاري والموبايل ليلي والحالات التفاعلية والـ Landing) طلّعت ملاحظة وحدة صغيرة بس، فهاي نقطة التوقف اللي يحددها الكيت (00 §7). آخر النتائج: 57 unit، 34 E2E، 11 QA gate، والـ bundle الرئيسي 188 KB gzip.

الجولة 1 (كل الشاشات، ديسكتوب + موبايل، نهاري + ليلي):
- **رقم مالي كان ينقطع بالموبايل** ("4,071,604" يطلع "1,604"): صار حجم رقم الـ KPI ينزل ويه عرض الكارد، وتحت 380px الكاردات عمود واحد (D18)، ووياه اختبار E2E على 320 و360 و390.
- الكاردات المتجاورة صارت بنفس الطول: المحفظة (التوزيع + الرصيد عبر الزمن)، الرؤى، وحاسبة الـ Landing.
- الرسم البياني الصغير ببطاقة السعر: انشال محور الأسعار (كان رقمه الأعلى مقصوص ويتداخل ويه تسمية آخر سعر).
- أزرار الميزانية صارت كلمات ("500 ألف د.ع"، "5 ملايين د.ع") وما تنكسر لسطرين.
- انشالت العناوين المكررة (الإعدادات، سجل العمليات، سجل المبيعات، عروضي)، لأن عنوان الصفحة موجود بالشريط العلوي.
- أسفل القائمة الجانبية: "تبديل الحساب التجريبي" و "تسجيل الخروج" كل واحد بسطر، و"غير موثّق" بدل الجملة الطويلة المقطوعة.
- علامات القوائم صارت مربعات (06 §3.2): شرط كلمة المرور و "ابدأ من هنا".
- التسجيل: محاذاة النصوص بالبداية، وماكو كلمة وحيدة بآخر السطر. الـ Segmented ما يتمدد على العرض كله.

الجولة 2 (الحالات التفاعلية: خطأ الدخول، نتائج المطابقة، المراجعة، التوثيق، الإيصال، النوافذ):
- **الحالة بعد التوثيق ما كانت تتحدث** ("غير موثّق" يبقى بالقائمة): الـ Mock كان يرجّع نفس الكائن اللي يعدّله، فالكاش ما يحس بالتغيير. صار يرجّع نسخة مثل الشبكة الحقيقية، ووياه اختبار unit و E2E.
- **رسم "رصيدك عبر الزمن"**: كان يعرض مدة ثانيتين بعد أول شراء. صار سلسلة يومية تبدي من صفر وتغطي أسبوع على الأقل.
- **أيقونة العين تغطي كلمة المرور**: الحقول LTR داخل صفحة RTL، فالأيقونة والمسافة المحجوزة لها صاروا بنفس الجهة.
- نافذة التوثيق: زر الإغلاق داخل الشريط الـ Navy بدل شريط أبيض فارغ.
- عناوين الشريط العلوي ما تنقطع بالموبايل (360 و390): حجم أصغر بالموبايل، السعر بدون "24K"، وعنوان مختصر للشراء. العناوين انتقلت لـ `ar.ts` (كانت مكتوبة بالكود).
- بيانات التجربة: الكمية المتاحة صارت تطابق المبيعات المسجلة.

الجولة 3 (التحميل، الأخطاء، الكيبورد، البائع بالموبايل):
- **الفشل كان يطلع كأنه صفر:** لوحة المستثمر ("0 عمليات"، "0 د.ع"، وقائمة "ابدأ من هنا" كأنه جديد)، لوحة البائع ("أضف عرضك الأول" وهو عنده عروض)، وسجل المبيعات. هسه أثناء التحميل يطلع skeleton، وبالفشل يطلع "تعذّر التحميل" ويه زر إعادة. القاعدة بـ `lib/queryStatus.ts` و `KpiCard status`.
- **"هذا العرض غير موجود" على أي خطأ:** صار بس لـ 404، وباقي الأخطاء "تعذّر تحميل العرض" ويه إعادة (`ListingLoadError`، بالتفاصيل والشراء وصفحة عرض البائع).
- مكوّن `ErrorState` واحد بدل 8 نسخ مختلفة الشكل.
- شاشة تحميل بالهوية (`PageLoader`: الشعار + مربعات التحميل) بدل شريط باهت، وقت استرجاع الحساب وأول تحميل للصفحة.
- رابط "انتقل إلى المحتوى" أول ضغطة Tab (WCAG 2.4.1). ترتيب Tab بالشراء منطقي، وإطار التركيز يبين على كل عنصر.
- مفاتيح اختبار بالـ Mock (README): `sila-mock-latency` و `sila-mock-fail`.
- اختبارات جديدة: `e2e/keyboard.spec.ts` (الشراء كامل بالكيبورد) و `e2e/states.spec.ts` (الفشل ما يطلع صفر).
- قرار مريم: تبقى إعادة المحاولة 3 مرات قبل رسالة الخطأ (الافتراضي بـ TanStack).

الجولة 4 (التابلت 768 و1024، الوضع الليلي لكل الصفحات):
- جدول العمليات وجدول "عروضي" بالـ 768 كان يطلع برّه الكارد ويقطع عمود المبلغ. الجداول صارت تبدي من 1024، وتحتها تطلع قائمة.
- بطاقات العروض بالـ 1024: عنوان "سعر مرجعي للغرام" ينكسر لسطرين والأرقام ما تبقى على نفس المستوى. صارت الأرقام تتحاذى من الأسفل.
- بطاقة التوقيع: بالعمود الضيق العنوان كان ينضغط لـ 3 أسطر، فصارت شارة "تم التحقق" تنزل تحته. وإذا ماكو توقيع: skeleton وقت التحميل، و"تعذّر التحميل" إذا فشل، بدل حقل فارغ بي زر نسخ.
- الوضع الليلي: كل الصفحات سليمة، ماكو تباين ضعيف أو ألوان ثابتة.

الجولة 5 (الـ Landing بالنهاري على 1440 و768 و390):
- قسم الأمان "تسعير موحّد": أعمدة العيارات الأربعة كانت بنفس الطول، فنسبة العيار ÷ 24 ما تبين. السبب إن العمود والتسمية بنفس المساحة، والمتصفح يضغطهم كلهم لنفس الطول. هسه الأعمدة 60 و55 و53 و45 (نفس نسبة 24:22:21:18).
- قسم البائعين: بطاقة العرض كانت تبين معطّلة (أزرار رمادية). صار بي وضع `demo` يخليها تبين حقيقية، والحاوية تبقى `inert`.
- طلب مريم: بحاسبة الأسعار صارت الحاسبة على اليمين، والسبائك وجدول العمولات على اليسار.

الجولة 6 (الزوايا: الدخول بالموبايل والليلي، "المزيد"، التنبيهات، 404، القائمة المصغّرة، "عروضي"):
- زر "حذف" بـ "عروضي" كان `disabled`، فشرحه ("لا يمكن حذف العروض...") ما يظهر لأن الزر ما ينضغط وما يوصله الكيبورد. صار `aria-disabled`: شكله معطّل وما يسوي شي، بس يوصله الماوس والكيبورد فيبين الشرح (الكيت 08 §9 يطلب الزر المعطّل ويه الشرح). ضفت متغيّر `inactive` بالـ Tailwind يغطي `disabled` و `aria-disabled`.
- التسجيل بالموبايل: كروت الملف الاستثماري كانت كل واحد بارتفاع 136 ثابت ومليانة فراغ. صارت بارتفاع محتواها بالموبايل.
- صفحة 404: صار بيها الشعار، والزر يودّي للوحة التحكم إذا المستخدم مسجّل دخول، وللصفحة الرئيسية إذا لا.

الجولة 7 (الشاملة الأخيرة):
- كروت الـ KPI بالموبايل (عمودين): "منذ أمس" كان ينقطع جنب النسبة، وتاريخ Premium ينقطع وتضيع السنة. بالكروت الضيقة صار "منذ أمس" يختفي جنب النسبة، والتاريخ يطلع بصيغة رقمية قصيرة ("19/10/2026"). فحصت كل نصوص الـ KPI من 360 لحد 1440 وماكو شي مقطوع.

**باقي بعد المرحلة 7:** الربط بالباكند الحقيقي لما يوصل الـ API Design النهائي (نقاط التأكيد بـ §0)، و `git init` لما ينصّب git.

---

### المرحلة 8 · الربط بالباكند (API_CONTRACT.md)

**الحالة: ✅ مكتملة (2026-09-24).** كل نداءات الفرونت حسب `Sila-Backend/docs/api/API_CONTRACT.md` بالضبط:
- الأنواع مولّدة من `openapi.json` (`npm run api:types`)، وطبقة `adapters.ts` تحوّل بين الشكلين (D21). كل المسارات تحت `/api` على `localhost:8010`.
- **الشراء (§5):** المعاينة = سعر مثبّت (`quote_token`) لـ 60 ثانية ويه عدّاد، والتأكيد يرسل التوكن ويه Idempotency-Key واحد لكل شاشة تأكيد. `PRICE_CHANGED` → معاينة جديدة ومفتاح جديد. انقطاع الشبكة → إعادة آمنة بنفس المفتاح. تحليل المخاطر يجي ويه المعاينة (أو `risk_insight_note`).
- **التوثيق (§5، §6):** بعد النجاح ينعاد الطلب الأصلي تلقائياً بنفس المفتاح، للشراء ونشر العرض (D22).
- **الإعدادات (`/api/config`):** الشرائح والرسوم والاشتراك من السيرفر (D26). انحذف `serverConfig.ts`.
- **الأسعار:** `is_stale` من السيرفر، والتغيّر اليومي يختفي لما يكون `null`، والرسوم البيانية من `/api/market/prices/history` (انحذف التاريخ الوهمي).
- **الصفحات:** السوق بـ "عرض المزيد"، والسجل و"عروضي" يجيبون كلشي (D25).
- **المحفظة:** `disclaimer` السيرفر يظهر دايماً جنب الرصيد، ورسالة المستثمر الجديد من السيرفر.
- **الأخطاء (§2):** رسالة السيرفر العربية تنعرض، و`details` تروح لحقولها، و`Retry-After` يحدد عدّاد محاولات الدخول. `INVALID_CREDENTIALS` و`EMAIL_ALREADY_EXISTS` بدل الأكواد القديمة.
- **الجلسة (§3):** الدخول يرجّع المستخدم ويه التوكنات، والتسجيل يرجّع الملف بس فيتبعه دخول، والـ refresh token بـ `sessionStorage` (D24)، و Premium بـ `is_premium_active`.
- **الـ Mock** انكتب من جديد حسب العقد نفسه، ويزرع بيانات الباكند بالضبط (D27)، فالاختبارات تشتغل عليه وتطابق السيرفر.
- تجربة على الباكند الحقيقي (بعد `seed`): 34 طلب كلها ناجحة، ويشمل شراء حقيقي 1 غ بحساب زينب (`preview` ثم `confirm` 201)، وماكو أخطاء بالصفحة.

**من مقترحاتنا بـ §3 أعلاه:** `title_ar` و `sparkline` و `transaction_ref` ما انضافت للعقد، فالعنوان ينبني من العيار، والـ sparkline من تاريخ الأسعار، والمرجع هو `id` (D23). `match_score` صار `score`.

## 5. قرارات تحتاج جوابك قبل/أثناء التنفيذ

1. **النشر بعد توثيق البائع:** تلقائي (Workflow 06 + UI Kit) أو إعادة ضغط (Workflow 02-ب)؟ **اقتراحي: تلقائي**، لأن الـ UI Kit أعلى أولوية بالسلوك المرئي، وأوثق القرار بـ `DECISIONS.md`.
2. **حذف** مجلد `New folder (2)/` المكرر؟
3. **`git init`** حتى نسوي commits لكل مرحلة؟
4. **رابط الباكند** (`VITE_API_URL`) وهل `/openapi.json` مفتوح حتى أطابق الأنواع؟
