# صِلة (Sila) · Frontend Decisions

> مطلوب حسب `00-AGENT-MASTER-PROMPT.md` §1: أي تعارض بين المصادر أو خروج مقصود عن الـ UI Kit ينكتب هنا مع السبب.
> الأولوية المعتمدة: Master Prompt ← UI Kit 01 إلى 08 ← Workflows (للسلوك) ← الـ skills ← الافتراضي.

| # | القرار | السبب | المرجع |
|---|---|---|---|
| D1 | **نشر العرض تلقائياً بعد توثيق البائع** (بدون إعادة ضغط "نشر"). | Workflow 02-ب يطلب إعادة الضغط، بس Workflow 06-أ و UI Kit 08 §5 (Sheet 2.3) يطلبون النشر التلقائي. الـ UI Kit أعلى أولوية. الشراء بالمقابل **ما يتنفذ تلقائياً أبداً** بعد KYC. | PLAN.md §5-1 |
| D2 | زر "اشترِ" ببطاقة العرض **Primary** مو ذهبي. | 08-ux §6 يحط زر شراء بكل بطاقة، بس 01-color §2 يمنع أكثر من زر ذهبي بالشاشة. شبكة بطاقات كلها ذهبية تكسر القاعدة، فالذهبي يبقى للـ CTA الوحيد بكل شاشة (مثلاً "أكّد الشراء"، "ابدأ المطابقة"). | 01-color §2, 08-ux §6 |
| D3 | أرقام بطاقة العرض بحجم `h4` (20px) بدل `h3` (26px). | عمودين بحجم h3 ما يدخلون ببطاقة عرضها الأدنى 288px بدون قص، والكيت نفسه يمنع قص الأرقام. | 04-layout §8.1-8.2, 02-typography §5.9 |
| D4 | شعار TradingView داخل الرسم البياني **باقي**. | رخصة Lightweight Charts تطلب الإسناد. إذا نريد نشيله من الرسم، لازم نحط رابط إسناد بصفحة "حول" أو التذييل (مؤجل لمرحلة الـ Landing/الإعدادات). | 06-style §7.1 |
| D5 | الـ Tooltip يتوسّط بـ `left-1/2 -translate-x-1/2`. | التوسيط الفيزيائي نفسه بالـ RTL والـ LTR، فهو مو مخالفة لقاعدة الخصائص المنطقية (القاعدة تخص الاتجاه). | 02-typography §6 |
| D6 | تسميات قصيرة بالـ tab bar للموبايل (`الرئيسية`، `المطابقة`، `المبيعات`، `إضافة`). | التسميات الكاملة تنكسر لسطرين بعرض 390px. السايدبار يحتفظ بالأسماء الكاملة. | 04-layout §9, 03-icon §5.1 |
| D7 | القيم المرجعية بالـ Tailwind عن طريق `@theme inline reference` مع تصفير الـ scales الافتراضية. | يضمن إن أي لون/حجم/radius خارج الكيت ما يطلع كـ utility أصلاً، والقيم الـ hex تبقى بـ `tokens.css` بس. | 01-color §10, 04-layout §2 |
| D8 | نصوص السيرفر العربية (AI، رسائل الأخطاء) تمر على `isolateFigures()`. | خوارزمية الـ bidi تقلب موضع "%" حول الأرقام داخل النص العربي ("%0.77"). عزل كل رقم بـ LRI/PDI يصلحها بدون ما نتحكم بنص السيرفر. | 02-typography §6 |
| D9 | انهيار السايدبار **بدون حركة عرض** (تبديل فوري). | 07-motion §6 يسمح بحركة `transform` و `opacity` بس، وتحريك `width` ممنوع. | 07-motion §6 |
| D10 | زر التأكيد بشريط الموبايل السفلي نصه "أكّد الشراء" بس، والمبلغ الكلي ظاهر بجنبه بنفس الشريط. | عرض 390px ما يكفي للمبلغ داخل زر بحجم مريح. المبلغ يبقى مقروء بجنب الزر، وبالديسكتوب الزر يكرر المبلغ كامل. | 08-ux §1.2, 04-layout T3 |
| D11 | إجراءات "عروضي" أزرار مباشرة بكل صف بدل قائمة منسدلة. | أوضح لجمهور 30 إلى 60 وأقل خطوات. زر الحذف ظاهر ومعطّل مع شرح، حسب الكيت. | 08-ux §9 Screen 6.2 |
| D12 | ترتيب السوق بدون "تحميل المزيد". | `GET /api/listings` ما بيه pagination بالتصميم الحالي. ينضاف لما يوصل الـ API النهائي. | 08-ux §6 Screen 3.2 |
| D13 | `LazyMotion` بـ `domMax` بدل `domAnimation`. | مؤشر الـ Segmented يستخدم `layoutId` (07-motion §3 #6)، وهذا يحتاج domMax. الميزات تتحمّل بعد أول رسم، فالحمولة الأولى نزلت 25%. | 07-motion §6 |
| D14 | Playwright يشتغل على Microsoft Edge المنصّب (`channel: 'msedge'`). | ما يحتاج تنزيل متصفح. اختبارات RTL/LTR مؤجلة لين تنضاف الإنكليزية (PLAN.md §0). | 00 §7 |
| D15 | عنوان الـ Hero يوقف عند `display-xl` (ما نستخدم `display-2xl`). | بـ 86px العنوان العربي ينكسر لـ 3 أسطر، وقاعدة "العنوان ≤ سطرين" أهم من درجة الحجم. | 08 §12 #2، 02 §5 |
| D16 | شعار الـ Nav يختفي لما الـ footer يبين. | حتى ما يطلع الشعار مرتين بنفس الشاشة. | 05 §6 |
| D17 | الـ Landing تتبع ثيم الجهاز، وبيها زر تبديل بالـ Nav (نفس اختيار التطبيق ويتذكره). بالنهاري: nav زجاجي أبيض، و wash فاتح (#d8eef6 → أبيض)، والشعار Navy. | طلب مريم. **يخالف** الكيت: 01 §4 و 08 §12 و 00 §5 تقول الـ Landing ليلي بس. | 01 §4، 08 §12 |
| D18 | رقم الـ KPI ما ينقطع أبداً: حجمه ينزل ويه عرض الكارد (container queries: `body` ثم `h4` من 12rem ثم `h3` من 14rem ثم `h2` من 17rem)، وتحت 380px الكاردات تصير عمود واحد (breakpoint `xs`). | بالموبايل كان "4,071,604" ينقطع ويطلع "1,604"، يعني رقم مالي غلط. ماكو حجم خط جديد، بس درجات من السلّم الموجود. | 02 §4، 04 §8.2 |
| D19 | بطاقة السعر بالـ Hero عليها ظل Navy (`--shadow-showcase`، ونسخة أغمق بالليلي). بس هي، والتطبيق يبقى بالـ hairlines. | طلب مريم (البطاقة باهتة). الكيت يحصر الظلال بالعناصر الطافية (06 §2)، والبطاقة هنا معروضة كمعاينة منتج طافية، فتنطبق عليها. | 06 §2، 04 §11 |
| D20 | زر "افتح حسابك" بالـ Nav صار ذهبي مثل زر الـ Hero، بس يطلع لما زر الـ Hero يطلع من الشاشة. والـ Nav يختفي بالنزول ويرجع بأول صعود (GSAP ScrollTrigger، بدون scroll listener). | طلب مريم: اللونين ما متناسقين. الذهبي يظهر لما زر الـ Hero يطلع من الشاشة، فتبقى قاعدة "زر ذهبي واحد بكل شاشة". | 01 §2، 08 §12، 07 §1 |
| D21 | الربط بالـ API يمر بطبقتين: `api/wire.ts` (أنواع مولّدة من `openapi.json` بـ `npm run api:types`) و `api/adapters.ts` (الحد الوحيد بين شكل السيرفر وشكل الواجهة). الأرقام العشرية (نصوص) تتحول لأرقام للعرض، والنسب المئوية ("1.16") لكسور، والطلبات تنرسل نصوص: الغرامات بـ 3 منازل والدينار بـ 2. الحساب بالواجهة (التقديرات والمجاميع) بـ `decimal.js`. | العقد §1 يطلب نصوص للمال والأوزان وحساب بمكتبة عشرية. التحويل للأرقام بالعرض بس وما يخسر دقة بهاي الأحجام. | API_CONTRACT §1 |
| D22 | بعد نجاح التوثيق (KYC) ينعاد الطلب الأصلي تلقائياً بنفس الـ Idempotency-Key: الشراء وينشر العرض. نافذة التوثيق تعرض "تم توثيق هويتك" لحظة (1.2 ثانية) وبعدين تكمل، وزر "إكمال الشراء" للي يريد يكمل فوراً. إذا انتهى السعر المثبّت بالأثناء (PRICE_CHANGED) تطلع معاينة جديدة بالأرقام الجديدة وما ينفّذ شي. | العقد §5 و §6 وطلب مريم. **يغيّر** سلوك 08-ux (كان يرجع للمراجعة بدون تنفيذ). | API_CONTRACT §5، §6 |
| D23 | عنوان العرض يُبنى من العيار ("ذهب عيار 21")، وخط السعر الصغير بالبطاقة هو تاريخ العيار الحقيقي لـ 24 ساعة، ورقم العملية بالإيصال هو `id` العملية (مختصر بالشاشة ويُنسخ كامل). | العقد ما بي `title_ar` ولا `sparkline` ولا `transaction_ref` (كانت مقترحات بـ PLAN §3). العقد نفسه يقول استخدم `1D` للـ sparklines. | API_CONTRACT §4 |
| D24 | الـ refresh token بـ `sessionStorage` (مو localStorage). يعني التحديث يحتفظ بالجلسة، بس فتح تبويب جديد يطلب دخول. | العقد §3.5. | API_CONTRACT §3 |
| D25 | الصفحات (limit/offset): السوق يجيب 20 وبي زر "عرض المزيد"، والعدد من `total`. سجل العمليات و"عروضي" يجيبون كل الصفحات (100 كل مرة) لأن مجاميع البائع (الذهب المباع، المبلغ المستلم) تنحسب منهم وما إلها endpoint. | العقد §1. يلغي D12 (بدون pagination) لأن الـ API صار يقسّم. | API_CONTRACT §1 |
| D26 | الرسوم والعمولات والاشتراك ومدة السعر المثبّت من `GET /api/config`، ولحد ما توصل ما ينعرض أي رقم (skeleton) وزر الدفع معطّل. تسميات الشرائح تجي من `label_ar` بالسيرفر (أضفناه بـ PR للباكند)، وإذا ما موجود تنبني بالعربي من الحدود لأن `label` إنكليزي وبيه شرطة. | العقد: "Display these values; never hard-code them". | API_CONTRACT §4 System |
| D27 | حسابات التجربة هي حسابات العقد §7 (كلمة السر `Sila@2026`)، والـ Mock يزرع نفس البيانات بالضبط (نفس الأشخاص والعروض والمشتريات)، فالتطبيق يتصرف نفس الشي بالوضعين. أزرار الدخول السريع تظهر بوضع الـ Mock وبنسخة التطوير بس. | حتى التجربة على الـ Mock تطابق الباكند. | API_CONTRACT §7 |
| D28 | نص الـ Hero يقدّم صِلة كمنصة استثمار أصول حقيقية تبدأ من الذهب: شارة فوق العنوان، وصف الأصول (الذهب متاح، العقارات والنفط قريباً) كعناصر نص مو أزرار، وسطر ثقة تحت الزر. زر الـ Hero صار "ابدأ استثمارك بالذهب"، وزر الـ Nav يبقى "افتح حسابك". الإيموجي صارت أيقونات Phosphor (عملات، مباني، قطرة)، والشرطة بالنص صارت فاصلة. | طلب مريم. **يخالف** الكيت: 08 §12 يكول "افتح حسابك" هو الاسم الوحيد لزر التسجيل. الإيموجي والشرطة ممنوعة بالكيت (03 §icons و 00 §3) ويفحصها الـ QA gate. | 08 §12، 03، 00 §3 |
| D29 | الـ Nav بالـ Landing **ما يختفي** بعد السكرول: بأعلى الصفحة يبقى عرض الحاوية وشفاف، وأول ما تنزل ينكمش لشريط زجاجي عائم على قد محتواه (بالموبايل عرض الشاشة ناقص 12px من كل جهة). الزوايا 13px حول أزرار 8px بحشوة 5px (نفس درج الـ radius)، والتحول حركة Motion `layout` واحدة (FLIP، transform بس). بالروابط سطح hover واحد ينزلق بينها (`layoutId`)، وشريط Sila Cut صغير يعلّم القسم الحالي، والروابط تمرّر بنعومة (`scrollIntoView`، وفوري مع reduced motion). الشعار ينحذف من الشريط لما الـ footer يبين (الشريط يعيد تحجيم نفسه)، والزر الذهبي بالـ Nav ينتظر لحد ما يطلع زر الـ Hero **وزر الـ CTA الأخير** من الشاشة. وتصحيح الشعار: كان `inline-flex` داخل عنصر inline فيجلس على خط النص ويطلع 4.5px فوق باقي العناصر، صار block-level `flex` بمكوّن `Logo` نفسه فينصلح بكل مكان (الـ Topbar أيضاً). | طلب مراجعة الواجهة. **يلغي** جزء "يختفي بالنزول" من D20. | 08 §12، 05 §6، 01 §2، 07 §1 |
| D30 | مؤشر اليد (`cursor: pointer`) قاعدة وحدة بـ `base.css` لكل عنصر يتفعّل (أزرار، `role` تفاعلية، `summary`، `select`، labels الـ checkbox/radio...)، و`not-allowed` لكل معطّل. | Tailwind v4 شال الافتراضي من الأزرار، فكانت عناصر كثيرة بدون يد. قاعدة وحدة بدل ما كل مكوّن يتذكرها. | 06 §5 |
| D31 | نظام الحركة صار مصدر واحد: `motion/tokens.ts` يصدّر `transition` (page/reveal/exit/fade) و`gsapT` بالثواني، و`T` بالـ GSAP مشتق منه. **التطبيق**: الصفحة تظهر بـ fade (opacity بس، حتى العناصر `fixed` تبقى مربوطة بالشاشة) وأقسامها تصعد بالتتابع 55ms (CSS `.page-stagger`، أول 8 بس، وما يبقى transform بعد النهاية). مؤشر العنصر النشط بالسايدبار وبالـ tab bar ينزلق (`layoutId`). الـ sparkline يترسم أول مرة ينشاف (clip-path بـ CSS بدل Anime `createDrawable`، لأن الخط `non-scaling-stroke` وقياس طوله بالـ viewBox المشدود غير دقيق)، وأرقام الـ KPI تعد من 0 أول مرة تنشاف (Anime، تكتب بعقدة النص مباشرة بدون re-render، وقارئ الشاشة يقرأ الرقم الحقيقي). **الـ Landing**: لغة ظهور وحدة بالـ markup: `data-reveal` للعناوين (الأسطر تطلع من قناع، كلمات كاملة مو حروف)، `data-reveal-block` للفقرات، `data-reveal-item` للعناصر اللي تدخل سوا (55ms). قسم "للبائعين" تحوّل من Motion لهذا النظام حتى عنوانه ينقسم. | طلب مراجعة الواجهة: حركة أرقى وأكثر سلاسة ومنهجية. المدد والمنحنيات كلها من التوكنز. | 07 §2، §3 #3 #6 #10 #12، §4.2، §4.6 |
| D32 | خيار `stackedHeight="auto"` بالـ `Card`: الارتفاع الثابت يشتغل من `lg` وفوق بس (لما البطاقة تشارك صف). بعمود واحد (موبايل وتابلت) تاخذ ارتفاع محتواها. مستخدم بلوحة التحكم (المطابقة، آخر العمليات)، المحفظة، وصفحة Premium (كان محتواها **ينقص** بالموبايل). وحشوة `spacious` صارت 20px بالموبايل و32px من التابلت. | الارتفاع الثابت هدفه محاذاة البطاقات جنب بعض (04 §8.1)، وبالعمود الواحد يترك فراغ كبير أو يقص المحتوى. | 04 §6، §8.1 |
| D33 | بطاقات قسم الأمان الأفقي صارت أصغر (400×460 بدل 576×576) وبنفس الهيكل: مربع أيقونة 52 ورقم الخطوة "01 / 05"، عنوان h4 ونص body، وتحتها صندوق ثابت الارتفاع للرسمة حتى الخمسة يتحاذون. أول وآخر بطاقة على حافة الحاوية (`track-inset`)، شريط تقدّم يتعبّى ويه الحركة، وحركة parallax صغيرة (24px) لمربع الأيقونة. | طلب مراجعة الواجهة (العناصر كبيرة ومو مرتبة). **يخالف** حجم اللوحات بـ 07 §4.5 (`--g-5`). | 07 §4.5 |
| D34 | الـ favicon ملف SVG مستقل بلون Deep Petrol ‏`#126782` (بدون metadata)، و`theme-color` بنفس اللون. | طلب مراجعة الواجهة. الأيقونة القديمة كانت `currentColor` فتطلع سوداء بالتبويب. | 05 §2 |
| D35 | **الأداء** (قياس على نسخة الإنتاج، موبايل متوسط = CPU أبطأ 4 مرات). **Landing:** (1) دخول الـ Hero صار CSS: الكلمات تنقسم بـ React وقت الرسم، بدل SplitText اللي كان يقيس كل كلمة ويجبر المتصفح يعيد تخطيط الصفحة كلها ويا كل كلمة (≈1.2 ثانية). (2) ظهور الأقسام (D31) صار CSS يشتغل بـ IntersectionObserver (كتابة بس بدون قراءة)، بدل tweens بـ GSAP كل وحدة تقرأ `getComputedStyle`. GSAP بقى للـ pin والـ scrub بس. (3) الرسم الأول يحتوي الـ Nav والـ Hero وشريط الأسعار بس، والباقي ينرسم كـ transition بعد دخول الـ Hero (≈1.3 ثانية) أو من أول تفاعل. (4) `content-visibility: auto` للأقسام اللي بعد كل الـ pins، والـ Nav يعيد التمركز على القسم بعد `scrollend`. (5) الـ Walkthrough يبني نسخة وحدة (ديسكتوب أو موبايل) بدل الثنتين. (6) الـ Nav يقيس الـ layout بس لما شكل الشريط يتغير (`layoutDependency`). (7) BrandMark يقيس كل القطع وبعدين يحرّك. **الشارت** صار chunk متأخر (`LazyPriceChart`) ويتبنى لما يقرب من الشاشة. **التطبيق:** الـ AppLayout صار chunk منفصل (زائر الـ Landing ما ينزّله)، وصفحات الدور تنحمّل بالخلفية بوقت الفراغ بعد أول شاشة (ما تنحمّل إذا مفعّل توفير البيانات)، وصفحة الدخول تحمّل الـ shell مسبقاً. Anime.js ينحمّل بس لما يشتغل العداد. **البناء:** مكتبات React والـ Query بملفات منفصلة (تبقى بالكاش بين الإصدارات)، وخطوط الشاشة الأولى (Plex 400 و700، Montserrat) صارت preload. **النتيجة:** ديسكتوب LCP من 2.0 إلى 0.5 ثانية، TBT من 304 إلى 110ms. موبايل TBT من 1.93 إلى 1.39 ثانية، والعنوان يرسم بـ 1.35 ثانية بدل 2.44. الـ bundle الأولي من 204 إلى ≈167KB gzip. التنقل بين الصفحات بدون أي تحميل كود. | طلب مراجعة الأداء. (1) و(2) **يخالفان** جدول المكتبات بـ 07 §1 (الـ Hero والظهور كانوا GSAP/Motion)، لأن نفس الحركة بـ CSS ما تكلّف شي. | 07 §1، §4، §6 |
| D36 | كارد قسم البائعين بصفحة الهبوط: إذا السوق بعده ما بيه ولا عرض، تطلع كارد مثال (عيار 21، بائع موثّق، 50 غ) بسعر الـ 21 الحي من السيرفر، وتحتها سطر "مثال توضيحي بسعر السوق الحالي.". أول ما يصير عرض حقيقي يحل محلها. إذا لا عروض ولا أسعار، العمود يختفي. | طلب مريم: السوق على Railway فاضي والكارد كانت تبقى تحميل للأبد. **يخالف** الكيت: 08 §12 #8 يطلب كارد العرض الحقيقي مو رسم توضيحي، فالكارد معلّمة بوضوح إنها مثال. | 08 §12 #8 |
